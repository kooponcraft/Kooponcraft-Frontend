import InitSwap from "@/components/init-swap";
import React from "react";

export const metadata = {
	title: "Swap Coupon - Kooponcraft",
};

const index = () => {
    return (
        <>
            <InitSwap />
        </>
    );
};

export default index;
