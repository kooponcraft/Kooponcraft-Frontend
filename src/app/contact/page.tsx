import Contact from "@/components/contact";
import React from "react";

export const metadata = {
	title: "Contact - Kooponcraft",
};

const index = () => {
	return (
		<>
			<Contact />
		</>
	);
};

export default index;
