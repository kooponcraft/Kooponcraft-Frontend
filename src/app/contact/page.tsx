import Contact from "@/components/contact";
import React from "react";

export const metadata = {
	title: "Kooponcraft Contact - NFT Marketplace",
};

const index = () => {
	return (
		<>
			<Contact />
		</>
	);
};

export default index;
