import Livebids from "@/components/dashboard/live-bids";
import React from "react";

export const metadata = {
	title: "Livebids - Kooponcraft",
};

const index = () => {
	return (
		<>
			<Livebids />
		</>
	);
};

export default index;
