import Leaderboard from "@/components/leaderboard";
import React from "react";

export const metadata = {
	title: "Leaderboard - Kooponcraft",
};

const index = () => {
    return (
        <>
            <Leaderboard />
        </>
    );
};

export default index;
