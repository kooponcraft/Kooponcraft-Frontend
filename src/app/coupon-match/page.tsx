import CouponMatch from "@/components/coupon-match";
import React from "react";

export const metadata = {
	title: "Coupon Match - Kooponcraft",
};

const index = () => {
    return (
        <>
            <CouponMatch />
        </>
    );
};

export default index;
