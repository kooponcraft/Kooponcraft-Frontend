import Login from "@/components/login";
import React from "react";

export const metadata = {
	title: "Kooponcraft Login - Next js NFT Marketplace",
};

const index = () => {
	return (
		<>
			<Login />
		</>
	);
};

export default index;
